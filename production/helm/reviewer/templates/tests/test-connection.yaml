apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reviewer.fullname" . }}-test-connection"
  labels:
    {{- include "reviewer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reviewer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
